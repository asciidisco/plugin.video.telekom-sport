language: python

python:
  - "2.7.10"

before_install:
  - cd .. && mv ./plugin.video.telekom-sport ./pvts && cd pvts
  - mv ./test/test_Constants.py ./test_Constants.py
script:
  - python setup.py develop
  - python test.py
after_sucess:
  - codeclimate-test-reporter

addons:
  apt:
    packages:
      - make

notifications:
  email:
    on_success: change
    on_failure: always
sudo: false
